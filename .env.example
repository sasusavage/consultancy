# Copy to .env and fill in real values. Never commit .env.
DATABASE_URL=postgresql://user:password@host:5432/dbname
SECRET_KEY=change-me-to-a-long-random-string

# Admin credentials are NOT set via environment. Create the first admin with:
#   flask create-admin
# (prompts for username + password; stored hashed in the database)
# Change it later from the admin dashboard or by re-running the command.
